import QtQuick 2.15
import QtQuick.Controls 2.15
import "."
/*pragma Singleton


Rectangle {
    visible: true
    Column {
        anchors.centerIn: parent

        Text {
            text: "Current Date: " + Calendar.getCurrentDate()
            font.pixelSize: 20
        }
    }
    QtObject {
        property string currentDate: Qt.formatDate(new Date(), "yyyy-MM-dd")

        function getCurrentDate() {
            return currentDate;
        }
    }
}
*/
